<Page x:Class="WpfApp.Pages.AccountPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AccountPage">

    <Grid>
        <Border Margin="20" Style="{StaticResource FloatingBorder}">
            <StackPanel>
                <TextBlock Text="Профиль" Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                <TabControl>
                    <TabItem Header="Аренды">Первая вкладка</TabItem>
                    <TabItem Header="Личные данные">Вторая вкладка</TabItem>
                    <TabItem x:Name="TabItemAdministration" Header="Администрирование" Visibility="Collapsed">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Button Margin="5" Content="Пользователи" Name="ButtonUsersPage" Click="ButtonUsersPage_Click"/>
                                <Button Margin="5" Grid.Column="1" Content="Транспорт" Name="ButtonVehiclesPage" Click="ButtonVehiclesPage_Click"/>
                                <Button Margin="5" Grid.Column="2" Content="Аренды" Name="ButtonBookingsPage" Click="ButtonBookingsPage_Click"/>
                                <Button Margin="5" Grid.Column="3" Content="Отзывы" Name="ButtonReviewsPage" Click="ButtonReviewsPage_Click"/>
                                <Button Margin="5" Grid.Column="4" Content="Оплаты" Name="ButtonPaymentsPage" Click="ButtonPaymentsPage_Click"/>
                            </Grid>
                            <Frame Grid.Row="1" x:Name="frameAdmin" NavigationUIVisibility="Hidden"/>
                        </Grid>
                    </TabItem>
                </TabControl>
            </StackPanel>
        </Border>
    </Grid>
</Page>
